# The main pipeline for de_project1

resources:
  pipelines:
    bronze_silver_earthquake_etl:
      name: Bronze_Silver
      configuration:
        catalog_name: ${var.catalog}
      libraries:
        - glob:
            include: ../src/DLT_pipelines/Bronze_Silver/transformations/**
      schema: project1_silver
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../src/DLT_pipelines/Bronze_Silver
